#include "core/Clock.h"

// Clock is mostly header-only; this translation unit ensures the library links.
namespace mt {

// Future: add global clock instance or clock distribution if needed.

} // namespace mt
